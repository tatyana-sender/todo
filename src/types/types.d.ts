export interface IconProps {
  width?: string,
  height?: string,
  color?: string
}

export interface TaskProps {
  name: string,
  status: string,
  id: string,
  description: string,
  createDate: string,
  deadline: string
};

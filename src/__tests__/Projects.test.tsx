import React from 'react';
import { fireEvent, render, screen } from '@testing-library/react';
import Projects from '@/components/layout/Projects';

describe('ProjectPage appearance', () => {
  it('should contains header and body', () => {
    //render(<Projects />);
    //const heading = screen.getByText(/Projects/i);
    //const sortButton = screen.getByText(/Sort/i);
    //expect(heading).toBeInTheDocument();
    //fireEvent.click(sortButton);
    //expect(screen.getByText(/Sorting will be later/i)).toBeInTheDocument();
  });
});
